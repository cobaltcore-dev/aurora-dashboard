---
"@cobaltcore-dev/aurora": minor
---

Add Ceph S3 bucket lifecycle configuration management. Implements full CRUD operations for lifecycle rules including expiration policies, noncurrent version expiration, and multipart upload cleanup. Includes a comprehensive UI for viewing, adding, editing, and deleting lifecycle rules from the bucket detail page. Storage-class transitions configured outside Aurora are preserved but not editable in the UI.
