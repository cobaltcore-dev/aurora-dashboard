---
"@cobaltcore-dev/aurora": patch
---

Add session monitor with polling and timer-based expiration check. Sessions now show an expiration modal instead of immediately redirecting to login, improving UX by explaining why the session ended.
