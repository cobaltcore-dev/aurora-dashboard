---
"@cobaltcore-dev/aurora": patch
---

Size the object storage tables to the space actually available below them instead of a fixed viewport offset. Custom banners, wrapped toolbars or breadcrumbs above a table no longer push the page past the viewport and produce a second scrollbar.
