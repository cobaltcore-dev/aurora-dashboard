---
"@cobaltcore-dev/aurora": patch
---

Update delete modals to use Modal built-in props and single-column DescriptionList, improve test accessibility using getByRole and getByLabelText queries, fix swap display to show "None" instead of "0 MiB", and replace array index keys with stable IDs
