---
"@cobaltcore-dev/aurora": minor
---

Add `policyDir` option to `createServer` to allow consumers to supply their own OpenStack policy files that take precedence over the built-in bundled policies.
