---
"@cobaltcore-dev/aurora": patch
---

Swift: the inline delete action in the container metadata editor now uses the
default button colour instead of primary-danger, consistent with the object
metadata editor.
